import React, { Component } from 'react';

class Subject extends Component {
    render() {
      return (
        <header>
              hello react!
        </header>
      );
    }
  }

export default Subject;